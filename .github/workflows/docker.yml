name: Docker

on:
  push:
    branches:
      - "master"
      - "main"
      # Staging image channel: pushes to `staging` build staging-<ts>-<sha>
      # tags that only the staging.paperclip.inc Flux ImagePolicy tracks.
      # Prod keeps tracking main-* — promotion is merging staging -> main.
      - "staging"
    tags:
      - "v*"
  # Fallback: pushes made with the default GITHUB_TOKEN (e.g. a bot's
  # `gh pr merge --auto`) never fire `on: push` workflow runs -- GitHub
  # suppresses it to prevent recursive workflow chains. workflow_dispatch is
  # explicitly exempt from that restriction. Confirmed root cause 2026-07-18:
  # PR #263's lockfile-refresh auto-merge (github-actions[bot], GITHUB_TOKEN)
  # landed on main with no docker build. refresh-lockfile.yml now force-triggers
  # this dispatch right after its own auto-merges land; this stays as the
  # general operator escape hatch for any other case (or manual force-build).
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    concurrency:
      group: docker-${{ github.ref }}
      cancel-in-progress: true
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=raw,value=main-{{date 'YYYYMMDDHHmmss'}}-{{sha}},enable={{is_default_branch}}
            type=raw,value=staging-{{date 'YYYYMMDDHHmmss'}}-{{sha}},enable=${{ github.ref == 'refs/heads/staging' }}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha

      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          cache-from: type=gha
          cache-to: type=gha,mode=max
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
